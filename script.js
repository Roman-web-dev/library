const library = [];

function Book(name, author, year, pages, uuid) {
     this.name = name;
     this.author = author;
     this.year = year;
     this.pages = pages;
     this.uuid - uuid;
}

function addBookToLibrary() {

}